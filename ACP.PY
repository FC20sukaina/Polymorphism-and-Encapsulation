class Rectangle:

    def calculate_area(length, width): return length * width 
    length = 10
    width = 5

    room_area= length*width
    print("My area is", {length*width})
class Triangle:

    def calculate_area(base, height): return 1/2 * base* height
    base = 4
    height = 8

    room_area= 1/2 *base*height
    print("My area is:", {1/2 * base* height})